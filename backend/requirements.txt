fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic==2.11.4
pydantic-settings==2.9.1
sqlalchemy==2.0.40
alembic==1.15.2
pymysql==1.1.1
cryptography==44.0.2
httpx==0.28.1
loguru==0.7.3
numpy==2.2.5
# ASR 主通道依赖：funasr 会自动拉起 torch，但 PyPI 默认源在部分平台装的是 GPU 版。
# 推荐两步安装（CPU 版，体积小）：
#   1) pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
#   2) pip install -r requirements.txt
funasr==1.4.6
yt-dlp==2025.6.9

# dev
pytest==8.3.5
